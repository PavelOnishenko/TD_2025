{
  "name": "td_2025",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "build:rgfn": "tsc -p rgfn_game/tsconfig.json",
    "watch:rgfn": "tsc -p rgfn_game/tsconfig.json --watch",
    "build:eva": "tsc -p eva_game/tsconfig.json",
    "watch:eva": "tsc -p eva_game/tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
