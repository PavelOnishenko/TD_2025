import Entity from '../../../engine/core/Entity.js';
import InputManager from '../../../engine/systems/InputManager.js';
import { Viewport } from '../types/engine';
import { AnimationState } from '../types/game';
export default class Player extends Entity {
    x: number;
    y: number;
    velocityX: number;
    velocityY: number;
    active: boolean;
    id: number;
    width: number;
    height: number;
    move: (deltaTime: number) => void;
    getBounds: () => {
        left: number;
        right: number;
        top: number;
        bottom: number;
    };
    checkCollision: (other: any) => boolean;
    health: number;
    maxHealth: number;
    attackDamage: number;
    isAttacking: boolean;
    invulnerable: boolean;
    facingRight: boolean;
    animationState: AnimationState;
    private attackTimer;
    private attackCooldownTimer;
    private invulnerabilityTimer;
    constructor(x: number, y: number);
    handleInput(horizontalInput: number, verticalInput: number, input: InputManager): void;
    private updateMovement;
    private updateAttack;
    private startAttack;
    update(deltaTime: number): void;
    private updateAttackTimer;
    private updateInvulnerability;
    takeDamage(amount: number): void;
    checkAttackHit(enemy: Entity): boolean;
    private isEnemyInAttackRange;
    private isFacingTowards;
    draw(ctx: CanvasRenderingContext2D, viewport?: Viewport): void;
    private drawPlayerBody;
    private drawPlayerFace;
    private drawHealthBar;
    private drawAttackIndicator;
}
//# sourceMappingURL=Player.d.ts.map